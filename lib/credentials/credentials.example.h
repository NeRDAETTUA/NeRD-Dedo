/* ---- Credentials Example Template ---- 
 * 
 * Copy this file inside the same folder and rename it to "credentials.h"
 * Then, open up "credentials.h" and fill in your WiFi and MQTT credentials.
 * 
 * This way, your credentials will be kept private and won't be uploaded to GitHub.
 * Avoiding any security issues.
 * 
 */

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

#define WIFI_SSID "your-ssid"
#define WIFI_PASS "your-password"
#define MQTT_SERVER "your-mqtt-server"
#define MQTT_USER "your-mqtt-user"
#define MQTT_PASS "your-mqtt-password"

#endif // CREDENTIALS_H